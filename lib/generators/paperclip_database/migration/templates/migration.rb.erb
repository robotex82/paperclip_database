class <%= migration_class_name %> < ActiveRecord::Migration
  def self.up
    create_table :<%= table_name %> do |t|
      t.integer    :attachable_id
      t.string     :attachable_type
      t.string     :attachable_name
      t.string     :style
      t.binary     :file_contents
    end
    add_index :<%= table_name %>, :attachable_id
    add_index :<%= table_name %>, :attachable_type
    add_index :<%= table_name %>, :attachable_name
  end

  def self.down
    drop_table :<%= table_name %>
  end
end
