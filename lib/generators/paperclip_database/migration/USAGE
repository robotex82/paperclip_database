Description:
    The generator generates a database migration for the paperclip database storage

Example:
    rails generate paperclip_database_migration

    This will create:
        db/migrations/YYYYMMDDHHmmss_create_table_paperclip_files.rb
