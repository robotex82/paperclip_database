Description:
    The generator generates a database migration for the paperclip database storage

Example:
    rails generate paperclip_database:migration install

    This will create:
        db/migrations/YYYYMMDDHHmmss_create_table_paperclip_dataabase_files.rb
